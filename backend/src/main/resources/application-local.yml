spring:
  datasource:
    url: jdbc:h2:file:./.data/restartquest;MODE=PostgreSQL;AUTO_SERVER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  flyway:
    enabled: false
