spring:
  datasource:
    url: jdbc:h2:mem:restartquest;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
restartquest:
  ai:
    provider: deterministic
